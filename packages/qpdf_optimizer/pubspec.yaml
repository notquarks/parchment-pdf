name: qpdf_optimizer
description: Local qpdf-backed PDF structural optimization for pdf_tools.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.2

hooks:
  build: true

dependencies:
  code_assets: ^1.2.0
  ffi: ^2.2.0
  hooks: ^2.0.2
  path: ^1.9.1

dev_dependencies:
  test: ^1.25.0
